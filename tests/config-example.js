export const config = {
    "JsonRpcProviderUrl": { // Erigon node rpc are highly recommended here.
        "mainnet": "http://{url}",
        "sepolia": "http://{url}"
    },
    "UserPrivateKey": "0x{key}"
}