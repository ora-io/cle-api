export const config = {
    "provider": { // Erigon node rpc are highly recommended here.
        "mainnet": "http://{url}",
        "sepolia": "http://{url}"
    },
    "SignerSecretKey": "0x{key}"
}